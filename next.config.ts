import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  /* config options here */
  allowedDevOrigins: ["https://achlorhydric-obnoxiously-kacy.ngrok-free.app"],
};

export default nextConfig;
